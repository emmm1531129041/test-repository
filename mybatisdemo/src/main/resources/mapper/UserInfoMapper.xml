<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatisdemo.mapper.UserInfoMapperXML">

    <!-- 插入用户（自动回填ID） -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert into user_info (username, password, age)
        values (#{username}, #{password}, #{age})
    </insert>

    <!-- 根据ID查询用户 -->
    <select id="selectUserById" resultType="mybatisdemo.model.UserInfo">
        select * from user_info where id = #{id}
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAllUsers" resultType="mybatisdemo.model.UserInfo">
        select * from user_info
    </select>

    <!-- 根据用户名模糊查询 -->
    <select id="selectUsersByUsername" resultType="mybatisdemo.model.UserInfo">
        select * from user_info where username like concat('%', #{username}, '%')
    </select>

    <!-- 更新用户名（方式1：直接传参） -->
    <update id="updateUserById">
        update user_info set username = #{username} where id = #{id}
    </update>

    <!-- 更新用户信息（方式2：传对象） -->
    <update id="updateUserById2">
        update user_info set username = #{userInfo.username}, age = #{userInfo.age}
        where id = #{userInfo.id}
    </update>

    <!-- 根据ID删除用户 -->
    <delete id="deleteUserById">
        delete from user_info where id = #{id}
    </delete>

    <!-- 批量删除用户 -->
    <delete id="deleteUsersByIds">
        delete from user_info where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <!-- 动态条件查询 -->
    <select id="selectUsersByCondition" resultType="mybatisdemo.model.UserInfo">
        select * from user_info
        <where>
            <if test="username != null and username != ''">
                and username like concat('%', #{username}, '%')
            </if>
            <if test="minAge != null">
                and age >= #{minAge}
            </if>
            <if test="maxAge != null">
                and age &lt;= #{maxAge}
            </if>
        </where>
    </select>

</mapper>